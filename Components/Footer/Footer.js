import {Text, View, ScrollView} from 'react-native';

export default function Footer() {
    return(
        <View>
            <ScrollView>
                <Text>Footer</Text>
            </ScrollView>
        </View>
    );
}